#include "main.h"
/**
 * _strlen - fonction
 *
 * Description: va permettre de print combiens de lettre dans une phrase
 * @s: pointeur
 *
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
int i = 0;
while
(s[i] != '\0') i++;

return (i);
}
