#include "main.h"
/**
 * _puts - fonction
 *
 * Description: print un autre pointeur
 *@str: pointeur
 *
 * Return: Always 0 (Success)
 */

void _puts(char *str)
{
{
	int i;

for (i = 0; str[i] != '\0'; i++)

_putchar(str[i]);
}
_putchar('\n');

}
