#include "main.h"
/**
 * _abs - permet de display des abdos ??? (en fait c pour montrer l'absolu )
 *@n: vieux truc x3
 * Description: exo qui a duré 100 ans à faire
 *
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
if (n > 0)
{
	return (n);
}
else if (n < 0)
{
	return (n * -1);
}
else
return (0);
}
