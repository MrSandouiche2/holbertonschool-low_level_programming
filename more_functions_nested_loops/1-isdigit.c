#include "main.h"

/**
 * _isdigit - fonction
 *
 *@c: le truc
 * Description: exo qui va return 1 si c'est un digit sinon ça discobombulate
 *
 *
 * Return: Always 0 (Success)
 */



int _isdigit(int c)


{

if (c >= '0' && c <= '9')
{
	return (1);
}
else
return (0);
}
